[build-system]
requires = [
  "ninja; platform_system!='Windows'",
  "scikit-build>=0.15.0",
  "setuptools>=42",
  "wheel",
]

build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
minversion = "6.0"
testpaths = [
  "test",
]
